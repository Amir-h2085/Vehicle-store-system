#include "Vehicle.h"

Vehicle::Vehicle(const string& brand, const string& modle, const string& color, int year, int maxSpeed) :
brand(brand), modle(modle), color(color), year(year), maxSpeed(maxSpeed){}